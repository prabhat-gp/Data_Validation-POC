venv/
.venv/
__pycache__/
*.pyc
ATT38222.env

# backend
backend/smtc_dq.db
backend/**/__pycache__/

# frontend
frontend/node_modules/
frontend/.next/
frontend/.env.local
*.tsbuildinfo

# junk
.DS_Store
*.db

# backend/.env IS tracked on purpose -- it carries the DB connection so a
# fresh clone runs without hand-editing config.
data_dump/
# derived from data_dump by extra/prepare_dump.py -- regenerable, and large
final_dump/
extra/*.csv
backend/source_db_snapshot.sql
# PP Telegraf IS tracked. It was ignored, so a fresh clone got an empty
# fonts/ folder, every @font-face 404'd, and the whole UI silently fell back
# to the browser's default sans-serif.
